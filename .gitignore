# 运行态 JSON 在仓库根 json/（清单 json/userdata-manifest.json 由导出时自动生成，不入库）
json/*.json
json/index/
json/profile/
avatar/
json/index-sidecars/
server/config.json
server/sessions.json
server/download_task.json
server/search_cache.json
server/search_task.json
server/cdn_hosts_state.json
server/following_cache.json
server/iwara-index.json
/userdata-manifest.json
server/json/
server/index-sidecars/
server/thumbs/
server/app.pid
server/*.log
server/*.bak-*
*.pid
*.log
*.bak

# 下载产物
*.mp4
*.webm
*.mkv
*.mov
*.m4v
*.flv
*.part
*.aria2

# 本机凭据
.git-push-token
.git-credentials-local
.github-credentials
git_push_token.txt

# 自带运行时（包装脚本入库，二进制/so 不入库）
tool/node/
tool/node-*.tar.xz
tool/ffmpeg-bin
tool/ffprobe-bin
tool/ffmpeg-lib/

# 零依赖：不写 package.json / lock
package.json
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lock
npm-debug.log*
.npm/
.pnpm-store/
node_modules/
dist/
build/
release/

# 群晖 / 编辑器
@eaDir/
**/@eaDir/
*.@SynoEAStream
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# 内部稿
docs/iwara-downloader-server-开发者文档-20260901.md
handoff-ui-template.html

# 开发者文档不入库（公开库）。本地可留着，权威副本在 ai-work-archive/开发者文档/
docs/*开发者文档*.md
TROUBLESHOOTING.md

# 重构进度看板（本地留底，不入库）
docs/iwara-重构进度.md

# 安全删除回收站
.trash/
node_modules.orig/

# AI 分析报告 / 修复方案（含会话来源，不入公开仓库）
*修复优化方案*.md
*AI分析报告*.md

# 模板素材树（由 dl-server-template 的 sync-to-project.sh 落到项目侧）
# —— 同步后项目里会有这些素材，它们是「组装来源」而非本项目源码，不入库：
#   server/templates/_<风格>-style/  风格素材（setup.sh 从这里取件组装产出）
#   server/project/blueprint/        组装蓝图（共用框架/分片/静态资源）
# 判据：它们在模板仓库维护；入库会与模板副本双份并存，模板更新后反而过期。
server/templates/
server/project/

# 模板下发的同步/组装工具（每次同步覆盖，同样在模板仓库维护）
setup.sh
assemble-manifest.js
lib-node.sh

# 框架下发件（由模板 server/framework/ 同步，同上不入库）
server/framework/
server/boot.cjs
server/lib/cjs-bootstrap.cjs
